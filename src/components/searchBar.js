import React, { Component } from "react";

class SearchBar extends Component {
  render() {
    return (
      <form>
          <input placeholder="search daily smart"/>
      </form>
    );
  }
}

export default SearchBar;
